dist: bionic
language: java
jdk: oraclejdk14

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - ./mvn clean install
  - bash <(curl -s https://codecov.io/bash)


cache:
  directories:
    - $HOME/.m2
